<table id="table-content">
    <thead>
        <tr id="show-columns">
            <th>#</th>
            <th>Nombre</th>
            <th>Ver</th>
            <th>Editar</th>
            <%if (locals.usuario.roles.roleName.toUpperCase() == "ADMINISTRADOR" ) { %>
                <th>Eliminar</th>
            <% } %>
        </tr>
    </thead>
</table>